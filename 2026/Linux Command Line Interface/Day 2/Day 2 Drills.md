# 🐧 Linux CLI — Day 2 Drills
## File & Text Manipulation

---

**Q1. What's the difference between `>` and `>>`?**

---

**Q2. You have a 2GB log file. Which command should you use to safely preview the first 20 lines, and why not `cat`?**

---

**Q3. Write a command that searches for the word "Fraud" inside `transactions.csv`, case-insensitive, showing line numbers.**

---

**Q4. What does `tail -f server.log` do, and when would you use it?**

`tail` prints the last 10 lines of a file. `-f` stands for **follow** — it keeps the terminal open and prints new lines as they are added to the file in real time.


---

**Q5. You want to search for the word "error" in every file inside a folder called `logs/`, including subfolders. Write the command.**

---

**Q6. Fill in the blank: `grep -v "DEBUG" logs.txt` shows all lines ____ DEBUG.**

---

**Q7. Write a command to count how many lines in `dataset.csv` contain "NULL".**

---

*Linux CLI for Data Scientists · Day 2 Drills · June 2026*
